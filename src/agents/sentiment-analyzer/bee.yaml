identity:
  role: worker
  name: sentiment-analyzer
  description: "分析文本情感倾向、情绪和态度"
  tags: [text, sentiment]

capabilities:
  - sentiment_analysis

model:
  name: glm-4.7

constraints:
  max_concurrent: 2
  timeout_default: 120
  queue_max: 100
  retry_max: 3

heartbeat:
  interval: 10
