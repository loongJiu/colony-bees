identity:
  role: worker
  name: recipe-matcher
  description: "根据食材匹配菜谱"
  tags: [dinner, recipe]

capabilities:
  - recipe_matching

model:
  name: glm-4.7

constraints:
  max_concurrent: 1
  timeout_default: 300
  queue_max: 100
  retry_max: 3

heartbeat:
  interval: 10
