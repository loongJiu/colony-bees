identity:
  role: worker
  name: keyword-extractor
  description: "从文本中提取关键词、标签和主题分类"
  tags: [text, keyword]

capabilities:
  - keyword_extraction

model:
  name: glm-4.7

constraints:
  max_concurrent: 2
  timeout_default: 60
  queue_max: 100
  retry_max: 3

heartbeat:
  interval: 10
