identity:
  role: worker
  name: nutrition-assessor
  description: "评估菜品营养成分和健康指数"
  tags: [dinner, nutrition]

capabilities:
  - nutrition_assessment

model:
  name: glm-4.7

constraints:
  max_concurrent: 1
  timeout_default: 300
  queue_max: 100
  retry_max: 3

heartbeat:
  interval: 10
