identity:
  role: worker
  name: ingredient-analyzer
  description: "分析食材成分、季节性、搭配建议"
  tags: [dinner, ingredient]

capabilities:
  - ingredient_analysis

model:
  name: glm-4.7

constraints:
  max_concurrent: 1
  timeout_default: 300
  queue_max: 100
  retry_max: 3

heartbeat:
  interval: 10
