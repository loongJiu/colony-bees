identity:
  role: worker
  name: comparison-analyzer
  description: "多维度对比分析方案、产品或技术"
  tags: [analysis, comparison]

capabilities:
  - comparison_analysis

model:
  name: glm-4.7

constraints:
  max_concurrent: 2
  timeout_default: 120
  queue_max: 100
  retry_max: 3

heartbeat:
  interval: 10
