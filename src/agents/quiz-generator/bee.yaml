identity:
  role: worker
  name: quiz-generator
  description: "根据学习内容生成各类测验题目"
  tags: [education, quiz]

capabilities:
  - quiz_generation

model:
  name: glm-4.7

constraints:
  max_concurrent: 2
  timeout_default: 120
  queue_max: 100
  retry_max: 3

heartbeat:
  interval: 10
