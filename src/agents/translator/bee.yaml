identity:
  role: worker
  name: translator
  description: "高质量多语言翻译，支持中英日韩法德西等主流语种"
  tags: [text, translation]

capabilities:
  - translation

model:
  name: glm-4.7

constraints:
  max_concurrent: 2
  timeout_default: 120
  queue_max: 100
  retry_max: 3

heartbeat:
  interval: 10
